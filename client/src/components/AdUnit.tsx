/**
 * AdUnit — Google AdSense stubs
 *
 * AdSense has been temporarily disabled pending publisher approval.
 * All components render null so no whitespace or layout shifts occur.
 * To re-enable: restore the AdUnit implementation and add the AdSense
 * script tag back to client/index.html.
 */

export default function AdUnit() {
  return null;
}

/** Horizontal banner ad — disabled pending AdSense approval */
export function AdBanner({ className: _className = "" }: { className?: string }) {
  return null;
}

/** In-article ad — disabled pending AdSense approval */
export function AdInArticle({ className: _className = "" }: { className?: string }) {
  return null;
}

/** Sidebar rectangle ad — disabled pending AdSense approval */
export function AdSidebar({ className: _className = "" }: { className?: string }) {
  return null;
}
